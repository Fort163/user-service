server:
  port: 8098
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: user-service
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka